export default function Methodology({ metadata }) {
 return <section className="panel methodology"><span className="eyebrow">SOURCES / METHODS / LIMITATIONS</span><h2>Understand the evidence</h2><p>Prepared from <strong>{metadata.notebookVersion}</strong>. {metadata.dataVintage}.</p><img src={`${import.meta.env.BASE_URL}pipeline.svg`} className="pipeline" alt="OpenDOSM and data.gov.my source data flow into the Python notebook, then static JSON and CSV exports, then the React dashboard."/><div className="method-grid"><div><h3>01 / Data preparation</h3><p>State-year and national-year data remain separate. The state panel spans 16 states and territories, with 176 rows across 2015–2025. Missing observations remain blank. The notebook’s corrected state-name filter preserves Pulau Pinang.</p><h3>02 / Sustainable Tourism Index</h3><p>The notebook standardizes five indicators: receipts per resident, length of stay, labour force participation, inverted unemployment and coastal good/excellent water quality. Directionally oriented PCA components are weighted by explained variance and rescaled to 0–100 across the full panel.</p><p>Three components retain approximately 86% of variance. The score is a relative composite, not an environmental certification.</p></div><div><h3>03 / Forecast validation</h3><p>The Random Forest learns next-year visitor growth. Training ends with the 2023→2024 transition; 2025 predictions use 2024 inputs. Growth is shrunk 50% toward no growth, then compared with published 2025 actuals. The saved results give 14.4% raw and 4.2% shrunk mean absolute error.</p><h3>04 / Associations, not causation</h3><p>Two-way fixed-effects models use lagged exposures and HC1 standard errors. Limited observations and untested confounding restrict interpretation. A near-threshold reverse association is suggestive evidence only.</p></div></div><h3>Coverage & caveats</h3><ul>{metadata.caveats.map(c=><li key={c}>{c}</li>)}</ul><h3>Data provenance</h3><p>{metadata.provenance.mode}</p><p>Sources: <a href="https://open.dosm.gov.my/" target="_blank" rel="noreferrer">OpenDOSM</a> · <a href="https://data.gov.my/" target="_blank" rel="noreferrer">data.gov.my</a>. State boundaries: <a href="https://gadm.org/" target="_blank" rel="noreferrer">GADM 4.1</a>, downloaded once and self-hosted. Refer to GADM’s terms for boundary reuse.</p><a className="button" href={`${import.meta.env.BASE_URL}data/Data.csv`} download>Download full Data.csv ↗</a></section>;
}
