export default function CausalInsights({ findings }) {
 return <section><div className="section-heading"><div><span className="eyebrow">EVIDENCE & UNCERTAINTY</span><h2>Tourism and coastal quality</h2></div></div><p className="muted mb-6">Lagged exposure models with state and year fixed effects. These observational findings do not establish causality.</p><div className="causal-grid">{findings.map((f,i)=><article className="panel finding" key={f.id}><span className="finding-number">0{i+1}</span><span className="eyebrow">{f.id} · N = {f.n}</span><h2>{f.title}</h2><h3>{f.result}</h3><p className="coefficient">{f.detail.replaceAll('_lag1',' (t−1)').replaceAll('_',' ')}</p><p className="caveat">{f.caveat}</p></article>)}</div><p className="chart-note">Findings use the full notebook panel, independent of dashboard filters. HC1 standard errors do not account for within-state serial correlation; the panel has only 13 states.</p></section>;
}
